# Exact package versions of the laptop environment (Python 3.12.13) that
# produced the numbers in DEVLOG.md and the meeting documents. Using the same
# versions on the cluster rules out a package change as the cause when a
# number moves.
#
#   .venv/bin/pip install -r cluster/requirements.txt

torch==2.13.0
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.9.0
matplotlib==3.11.1
h5py==3.16.0
pytest==9.1.1

# jetnet is only needed for the FPD/KPD metrics; the rest is what it imports.
jetnet==0.2.5
energyflow==1.4.0
numba==0.66.0
tables==3.11.1
pandas==3.0.3
awkward==2.10.0
coffea==2026.7.0
pyyaml==6.0.3
requests==2.34.2
tqdm==4.69.0
